# [BO][알림관리] 알림 등록하기 - 앱푸시

## 1. 화면 개요

### 1-1. 화면 목적
백오피스 내 알림관리에서 앱푸시 알림을 등록/수정/확인하는 팝업 화면이다.

사용자는 본 화면에서 아래 항목을 설정할 수 있다.
- 알림 구분
- 알림 유형
- 알림명 / 타이틀 / 본문
- 푸시링크
- 이미지(추후)
- 발송일 또는 즉시 발송
- 타겟 조건

---

### 1-2. 화면 유형
팝업형 모달 화면으로 제공한다.

---

### 1-3. 화면 영역 구분
화면은 크게 아래 3개 영역으로 구성한다.

#### ① 기본 정보 입력 영역
팝업 상단 영역  
알림 기본 정보와 발송 정보를 입력하는 영역

포함 항목:
- 알림 구분
- 알림 유형
- 알림명
- 타이틀
- 본문
- 푸시링크
- 이미지
- 발송일
- 즉시 발송

#### ② 타겟 설정 영역
팝업 중단 영역  
푸시 발송 대상을 조건별로 설정하는 영역

포함 항목:
- 성별
- 연령
- 구매 장르
- 발송 OS
- 성향

#### ③ 버튼 영역
팝업 하단 영역  
화면 모드에 따라 `취소 / 등록`, `취소 / 수정`, `확인` 버튼을 노출하는 영역

---

## 2. 화면 모드

### 2-1. 등록 모드
신규 앱푸시 알림을 생성하는 상태

- 하단 버튼: `취소`, `등록`

### 2-2. 수정 모드
기 등록된 앱푸시 알림 중 발송 상태가 `예약`인 경우 수정하는 상태

- 하단 버튼: `취소`, `수정`

### 2-3. 확인 모드
발송 상태가 `예약`이 아닌 경우 상세 내용을 확인하는 상태

- 하단 버튼: `확인`

---

## 3. 화면 배치 설명

### 3-1. 기본 정보 입력 영역
기본 정보 입력 영역은 세로 폼 형태로 배치한다.

각 항목은 기본적으로 아래 순서로 노출한다.
1. 알림 구분
2. 알림 유형
3. 알림명
4. 타이틀
5. 본문
6. 푸시링크
7. 이미지
8. 발송일
9. 즉시 발송

---

### 3-2. 푸시링크 영역
푸시링크 영역은 라디오 선택에 따라 입력 UI가 달라진다.

#### 작품 링크 선택 시
- `찾아보기` 버튼이 노출된다.
- 작품 검색 팝업을 통해 작품 1개를 선택할 수 있다.
- 선택 완료 후 작품 정보가 입력창에 노출된다.
- 작품 상세 URL이 하단에 표시된다.

#### 연결 링크 선택 시
- URL 직접 입력창이 노출된다.
- 이벤트 상세 / 특정 페이지 등 직접 연결 가능한 URL을 입력한다.

---

### 3-3. 발송일 영역
발송일 input 클릭 시 달력 + 시간 선택 레이어를 노출한다.

시간 선택 UI는 아래 구조를 사용한다.
- 날짜 선택
- 시(hour) 선택
- 분(minute) 선택

즉시 발송 체크 시 발송일 입력은 비활성화된다.

---

### 3-4. 타겟 설정 영역
타겟 설정 영역은 조건별로 한 줄씩 배치한다.

각 줄은 아래 구조를 따른다.
- 항목명
- `[전체]` 버튼
- 세부 선택 버튼들

예시:
- 성별: 전체 / 남성 / 여성
- 연령: 전체 / 10대 / 20대 / 30대 / 40대 / 50대 / 60대 이상

---

### 3-5. 버튼 영역
버튼 영역은 우측 하단 정렬로 배치한다.

#### 등록 모드
- 취소
- 등록

#### 수정 모드
- 취소
- 수정

#### 확인 모드
- 확인

---

## 4. 필드 상세 정의

## 4-1. 알림 구분
- 컴포넌트: 라디오버튼
- 필수 여부: 필수
- 선택 항목:
  - 앱푸시
  - 인앱
  - 스튜디오
  - 파트너 사이트

### 앱푸시 선택 정책
- 본 화면은 `앱푸시` 선택 시 기준으로 정의한다.
- 유저 디바이스에서 OS(iOS/AOS) 시스템 기본 UI 정책에 따라 팝테일 로고 아이콘이 자동 노출된다.

---

## 4-2. 알림 유형
- 컴포넌트: 라디오버튼
- 필수 여부: 필수
- 선택 항목:
  - 마케팅(광고)
  - 정보성(공지)

### 마케팅(광고)
- 마케팅 수신동의 회원에게만 발송한다.
- 발송 시간이 야간(21:00 &#126; 익일 08:00)인 경우,
  `야간 광고성 정보 수신 동의` 회원에게만 발송한다.
- 본문 앞에 `(광고)` 문구를 자동 생성한다.
- 수신거부 경로 표시는 추후 적용한다.

### 정보성(공지)
- 전체 회원 대상 발송
- 실제 발송 시 탈퇴/휴면/푸시토큰 미보유 회원 제외 여부는 별도 확인 필요

---

## 4-3. 알림명
- 컴포넌트: input
- 필수 여부: 필수
- placeholder: `최대 20자`
- 입력 가능 문자:
  - 한글
  - 영문 대/소문자
  - 숫자
  - 특수문자
- 길이 제한:
  - 최소 2자
  - 최대 20자
- 길이 기준:
  - 공백 포함
- 20자 초과 시 입력 불가

---

## 4-4. 타이틀
- 컴포넌트: input
- 필수 여부: 필수
- placeholder: `최대 20자`
- 입력 가능 문자:
  - 한글
  - 영문 대/소문자
  - 숫자
  - 특수문자
  - 이모지
- 길이 제한:
  - 최소 2자
  - 최대 20자
- 길이 기준:
  - 공백 포함
- 20자 초과 시 입력 불가

### 노출 정책
- 실제 푸시 노출 시 본문보다 크고 굵은 폰트(Bold)로 표시한다.

---

## 4-5. 본문
- 컴포넌트: input
- 필수 여부: 필수
- placeholder:
  - 마케팅(광고): `(광고) 최대 50자`
  - 정보성(공지): `최대 50자`
- 입력 가능 문자:
  - 한글
  - 영문 대/소문자
  - 숫자
  - 특수문자
  - 이모지
- 길이 제한:
  - 최소 2자
  - 최대 50자
- 길이 기준:
  - 공백 포함
- 50자 초과 시 입력 불가

### 마케팅(광고) 선택 시 정책
- 본문 앞에 `(광고)` 문구를 자동 생성한다.
- `(광고)` 문구는 readonly 처리한다.
- 사용자는 수정 또는 삭제할 수 없다.

---

## 4-6. 푸시링크
- 컴포넌트:
  - 라디오버튼
  - input
  - 버튼
- 필수 여부: 필수
- 선택 항목:
  - 작품 링크
  - 연결 링크

### 4-6-1. 작품 링크
#### 화면 동작
- 작품 링크 선택 시 `찾아보기` 버튼을 노출한다.
- `찾아보기` 버튼 클릭 시 작품 검색 팝업을 호출한다.
- 작품은 1개만 선택 가능하다.
- `등록` 클릭 시 선택한 작품 정보를 inputbox에 반영한다.
- 작품 정보는 아래 형식으로 노출한다.
  - 작품명(작품코드/작품타입)

예시:
- `내머리속소드마스터((WNFM260G0653SKR/웹소설))`

#### URL 표시
- 작품 선택 후 작품 상세 URL을 하단에 표시한다.
- 예시:
  - `https://poptale.ai/detail/1770613967041`

### 4-6-2. 연결 링크
#### 화면 동작
- 연결 링크 선택 시 URL 직접 입력 input을 노출한다.
- 특정 작품 열람 페이지, 이벤트 상세 페이지 등으로 직접 이동 처리한다.

예시:
- `https://poptale.ai/event/content_26`

### 4-6-3. 공통 정책
- 작품 링크와 연결 링크는 동시 선택 불가
- 선택한 링크 타입 1개만 저장 가능
- URL validation 범위는 개발 협의 필요

---

## 4-7. 이미지
- 상태: 추후 적용
- 현재 화면에서는 비활성 또는 미노출 처리 여부 개발 협의 필요

### 추후 정책
- 등록 방식: 파일 검색기
- 파일 형식: jpg, png
- 권장 비율: 2:1
- 권장 사이즈:
  - 1024 x 512 px
  - 800 x 400 px
- 등록 후 파일명 노출
- 파일명 클릭 시 미리보기 노출
- 삭제 아이콘 노출

---

## 4-8. 발송일
- 컴포넌트: 날짜/시간 입력
- 필수 여부:
  - 즉시 발송 미선택 시 필수
- 입력 방식:
  - 텍스트 입력 가능
  - 캘린더 선택 가능
- 노출 포맷:
  - `YYYY-MM-DD (요일) HH:mm:ss`

### 시간 선택 정책
- 시:
  - 00시 &#126; 23시
  - 1시간 단위
- 분:
  - 00분 &#126; 50분
  - 10분 단위

### 제한 정책
- 오늘 이전 날짜 선택 불가
- 현재 시각 이전 시간 선택 불가

### 발송 정책
- 설정한 발송일시에 자동으로 앱푸시 발송 처리한다.

---

## 4-9. 즉시 발송
- 컴포넌트: 체크박스

### 선택 시
- 등록 버튼 클릭 후 즉시 발송 처리한다.
- 발송일 입력 영역은 disabled 처리한다.
- 안내 툴팁 노출:
  - `즉시 발송 시 실제 발송까지 약 1&#126;2초 소요됩니다.`

### 해제 시
- 발송일 입력 영역을 enabled 처리한다.

---

## 5. 타겟 설정 정책

## 5-1. 공통 정책
- 타겟 항목은 모두 필수값이다.
- 각 항목의 기본값은 `전체`이다.
- 각 항목은 Toggle 버튼 방식으로 동작한다.
- 다중 선택 가능하다.
- 항목 간 조건은 AND이다.
- 항목 내부 선택은 OR이다.
- 선택한 타겟 조건에 속하는 회원만 발송 대상에 포함한다.

---

## 5-2. 전체/세부 버튼 동작 정책
### 기본 규칙
1. `[전체]`가 선택된 상태에서 세부 버튼 클릭 시:
   - `[전체]`는 자동 해제
   - 클릭한 세부 버튼만 활성화

2. 사용자가 세부 버튼을 하나씩 눌러 해당 줄의 모든 세부 버튼을 모두 선택하는 순간:
   - 세부 버튼들은 모두 해제
   - `[전체]` 버튼만 활성화 상태로 자동 전환

3. 빈 값 방지:
   - 아무 값도 선택되지 않은 상태는 허용하지 않음
   - 마지막으로 켜져 있는 버튼은 다시 눌러도 해제되지 않음

---

## 5-3. 성별
- 기준 데이터:
  - 회원관리 > 상세보기 > 성별
- 선택 항목:
  - 전체
  - 남성
  - 여성

---

## 5-4. 연령
- 기준 데이터:
  - 회원관리 > 상세보기 > 생년월일
- 선택 항목:
  - 전체
  - 10대
  - 20대
  - 30대
  - 40대
  - 50대
  - 60대 이상

---

## 5-5. 구매 장르
- 기준 데이터:
  - 유저가 캔디를 사용하여 작품을 구매(소장/대여)한 이력의 작품별 장르 데이터
- 포함 기준:
  - 유료 재화(캔디) 사용 이력만 인정
  - 무료 열람 제외
  - 정산/비정산 캔디 모두 포함

- 선택 항목:
  - 전체
  - 로맨스
  - 로판
  - 판타지
  - BL
  - 없음

### 정책
1. 구매 장르를 1개 이상 선택한 경우
   - 선택한 장르 중 하나라도 구매 이력이 있으면 발송 대상 포함
   - 장르 간 OR 조건으로 처리

2. `전체` 선택 시
   - 구매 여부와 상관없이 전체 회원 발송

3. `없음` 선택 시
   - 장르 구매 이력이 없는 회원만 발송 대상

### 예시
#### 발송 O
- 회원 A:
  - 과거 `로맨스`, `로판` 장르 작품 소장/대여 이력 보유
- 구매 장르 설정:
  - `로맨스`, `판타지`
- 결과:
  - `로맨스` 구매 이력이 있으므로 발송 대상 포함

#### 발송 X
- 회원 B:
  - 과거 `로맨스`, `로판` 장르 작품 소장/대여 이력 보유
- 구매 장르 설정:
  - `무협`, `판타지`
- 결과:
  - 선택 장르 구매 이력이 없으므로 발송 대상 제외

> 참고: 실제 UI 선택 항목은 `전체, 로맨스, 로판, 판타지, BL, 없음` 기준으로 노출한다.

---

## 5-6. 발송 OS
- 기준 데이터:
  - 회원관리 > 상세보기 > 기기등록정보 > OS
- 선택 항목:
  - 전체
  - AOS
  - iOS

---

## 5-7. 성향
- 기준 데이터:
  - 회원관리 > 상세보기 > 성향/분위기
- 선택 항목:
  - 전체
  - 남성향
  - 여성향

---

## 6. 버튼 영역 정책

## 6-1. 취소 버튼
### 공통 동작
- 팝업 종료

### 작성 내역 존재 시
- 확인 팝업 노출

#### 메시지
- M1: `작성 내역이 있습니다. 취소 하시겠습니까?`

#### 버튼
- 확인: 팝업 종료
- 취소: 현재 화면 유지

---

## 6-2. 등록 버튼
- 적용 모드: 등록 모드
- 필수값 미입력 시 disabled
- 필수값 충족 시 enabled

### 클릭 시
- 입력값 검증 수행
- 저장 성공 시 팝업 종료
- 목록 갱신
- 완료 메시지 노출

#### 메시지
- TM1: `알림 등록이 완료 되었습니다.`

---

## 6-3. 수정 버튼
- 적용 모드: 수정 모드
- 발송 상태가 `예약`인 경우만 노출

### 클릭 시
- 입력값 검증 수행
- 저장 성공 시 팝업 종료 또는 목록 리다이렉트 후 목록 갱신
- 완료 메시지 노출

### 예약 시간 도래 방어 로직
- 수정 요청 시점에 이미 발송되었거나 발송 시작된 경우 수정 불가 처리

#### 메시지
- M2: `푸시 알림이 이미 발송(또는 발송 시작)되어 수정이 불가능합니다.`

#### 완료 메시지
- TM2: `알림이 수정 되었습니다.`

---

## 6-4. 확인 버튼
- 적용 모드: 확인 모드
- 발송 상태가 `예약`이 아닐 경우 노출
- 상세 확인 후 화면 닫기 용도

---

## 7. 상태값 정책
- 예약: 수정 가능
- 발송 중: 수정 불가
- 발송 완료: 수정 불가
- 발송 실패: 수정 가능 여부 별도 확인 필요

---

## 8. 유효성 검증

### 8-1. 필수값
- 알림 구분
- 알림 유형
- 알림명
- 타이틀
- 본문
- 푸시링크
- 발송일 또는 즉시 발송
- 타겟 설정 전체 항목

### 8-2. 입력 검증
1. 알림명 2&#126;20자
2. 타이틀 2&#126;20자
3. 본문 2&#126;50자
4. 발송일 현재 시각 이전 입력 불가
5. 작품 링크는 1개만 선택 가능
6. 타겟 설정 빈 값 불가
7. 마케팅(광고) + 야간 발송 시 야간 광고성 정보 수신동의 회원만 대상 산정

---

## 9. 발송 대상 산정 정책

### 9-1. 알림 유형 기준
#### 마케팅(광고)
- 마케팅 수신동의 회원만 포함
- 야간 시간대 발송 시 야간 광고성 정보 수신동의 회원만 포함

#### 정보성(공지)
- 전체 회원 대상

---

### 9-2. 타겟 기준
- 성별 / 연령 / 구매장르 / 발송OS / 성향 조건은 항목 간 AND
- 각 항목 내부 선택값은 OR

---

## 10. 메시지 정의

| 메시지 ID | 구분 | 문구 |
|---|---|---|
| M1 | 확인 팝업 | 작성 내역이 있습니다. 취소 하시겠습니까? |
| M2 | 오류 팝업 | 푸시 알림이 이미 발송(또는 발송 시작)되어 수정이 불가능합니다. |
| TM1 | 완료 메시지 | 알림 등록이 완료 되었습니다. |
| TM2 | 완료 메시지 | 알림이 수정 되었습니다. |

---

## 11. 확인 필요사항
1. 정보성(공지) 발송 시 탈퇴/휴면 회원 제외 여부
2. 푸시 발송 기준이 푸시 토큰 보유 회원인지 여부
3. 연결 링크 외부 URL 허용 여부
4. URL 유효성 검증 범위
5. 이미지 기능 노출 방식(비활성/미노출)
6. 발송 실패 상태 수정 가능 여부
7. 수정 성공 시 화면 처리 방식
   - 팝업 종료 후 목록 갱신
   - 목록 페이지 리다이렉트

---

## 12. 완료 조건
1. 화면이 기본 정보 입력 / 타겟 설정 / 버튼 영역으로 구분되어 노출되어야 한다.
2. 등록/수정/확인 모드에 따라 하단 버튼 구성이 달라져야 한다.
3. 마케팅(광고) 선택 시 본문 앞에 `(광고)`가 자동 노출되어야 한다.
4. 즉시 발송 체크 시 발송일 입력이 비활성화되어야 한다.
5. 푸시링크 선택값에 따라 작품 검색 UI 또는 URL 입력 UI가 노출되어야 한다.
6. 타겟 설정 각 항목은 기본값 `전체`로 노출되어야 한다.
7. 타겟 전체/세부 버튼 토글 로직이 정책대로 동작해야 한다.
8. 구매 장르 `전체` 선택 시 구매 여부와 관계없이 전체 회원 대상이어야 한다.
9. 구매 장르 `없음` 선택 시 구매 장르 이력이 없는 회원만 포함되어야 한다.
10. 예약 상태에서만 수정 가능해야 한다.
11. 발송 시작 이후 수정 시 M2 메시지가 노출되어야 한다.
12. 등록 완료 시 TM1, 수정 완료 시 TM2 메시지가 노출되어야 한다.
